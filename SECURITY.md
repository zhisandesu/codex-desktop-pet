# 安全与隐私

## 凭据边界

- 发布包不读取或导出开发者的 Windows Credential Manager 数据。
- 用户的方舟凭据保存在自己机器上的 `XiaobianPet/ArkAgentPlanTts` 或 `XiaobianPet/ArkModelApi` 凭据项，或由用户自行设置进程环境变量。
- Codex 登录由用户的本机 Codex 管理。不得把 `.codex`、`auth.json`、本地会话、任务日志、录音或 `%LocalAppData%\XiaobianPet` 上传到 GitHub。
- `settings.json` 是用户运行数据，不是可以复制到发行包的示例配置。

## 打包防线

源码采用允许清单，不递归复制整个开发目录；只导出当前代码、运行 PNG 和必要的动画配置。生成动画时留下的响应、签名 URL、提示词归档、预览视频、旧 dist、截图及项目外数据不包含在内。PNG 文本/EXIF 元数据在导出时移除，像素压缩数据和透明度保持原样。

`python tools/release.py scan` 会阻止不在清单中的源码文件、常见私有文件、常见密钥模式、字面凭据、签名下载链接和个人用户目录路径；报错只打印相对路径、行号与规则，不打印命中值。构建后再次扫描分发目录并检验 ZIP CRC。

扫描是辅助防线，不能保证识别所有未知格式的秘密。发布前仍需人工审查 Git 暂存内容，启用 GitHub 可用的 secret scanning / push protection，不提交真实密钥作测试。

## 报告问题

不要在公开 Issue 粘贴密钥、录音或完整聊天记录。仓库启用私密漏洞报告后优先使用该入口；在入口未启用前，仅公开描述不含敏感内容的复现步骤。若曾泄露密钥，应在服务方撤销并更换，删除文件或增加 gitignore 不会清除历史泄漏。

## 第三方构建路径例外

NAudio 2.2.1 的 7 个原始 DLL 含供应商构建路径。发布扫描仅对已核对 NuGet 原包的精确 SHA256 豁免该路径规则；文件改变或命中密钥规则仍会失败，不豁免本项目源码或本机路径。
